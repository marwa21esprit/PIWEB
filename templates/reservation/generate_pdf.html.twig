<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservation Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }
        h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }
        img.reservation-image {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }
        p {
            font-size: 16px;
            margin-bottom: 10px;
        }
        strong {
            font-weight: bold;
        }
        .qr-code {
            max-width: 200px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reservation Details</h1>
        <img src="{{ asset('uploads/' ~ reservation.imagesrc) }}" alt="Reservation Image" class="reservation-image">
        <p><strong>Name:</strong> {{ reservation.name }}</p>
        <p><strong>Email:</strong> {{ reservation.email }}</p>
        <p><strong>Number of Places:</strong> {{ reservation.nbPlaces }}</p>
        <p><strong>Event Name:</strong> {{ reservation.namee }}</p>
        <p><strong>Event Date:</strong> {{ reservation.idEvent.dateevent|date('Y-m-d') }}</p>
        <p><strong>Reserved by:</strong> {{ reservation.idUser ? reservation.idUser.name : 'Unknown User' }}</p>
        <p><strong>Reserved at:</strong> {{ paiement.heureP|date('Y-m-d H:i:s') }}</p>
        <p><strong>Montant Total:</strong> {{ reservation.nbPlaces * reservation.eventprice }} TND</p>
        <p><strong>QR Code:</strong></p>
        <img src="{{ qrCodeUrl }}" alt="QR code" class="qr-code">
    </div>
</body>
</html>
